import React from "react";
import { ConnectionRequiredScreen } from "../screens";

/**
 * Container for the {@link ConnectionRequiredScreen}
 */
export const ConnectionRequiredContainer: React.FC = () => {
  return <ConnectionRequiredScreen />;
};
